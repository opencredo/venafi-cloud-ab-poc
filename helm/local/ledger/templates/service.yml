apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.serviceName }}-svc
  namespace: {{ default "core" .Values.namespace }}
spec:
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: {{ .Values.serviceName }}
